<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth LE mesh permissions — declared to MATCH bitchat-android exactly.
         Core BitChat's BluetoothPermissionManager requires ACCESS_FINE/COARSE
         LOCATION on ALL API levels (not just <31), and BLUETOOTH_SCAN is declared
         WITHOUT neverForLocation because the core does use location. Capping
         location at maxSdkVersion=30 (or using neverForLocation) makes the core
         report "Has Permissions: false" on Android 12+ and the mesh never starts. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Legacy Bluetooth permissions (API < 31). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Location required by the core on ALL API levels. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Foreground service — keeps the BLE mesh alive in the background
         (MeshForegroundService). POST_NOTIFICATIONS is required on API 33+ for
         the persistent notification the FGS must show. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application>
        <!-- Vendored Core BitChat foreground service that keeps the mesh running
             in the background. Started/stopped by MeshSdkModule. -->
        <service
            android:name="com.bitchat.android.service.MeshForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync|location" />

        <!-- Notification-tap trampoline. Core hardcodes its tap PendingIntents to
             com.bitchat.android.MainActivity (compiled but not declared in the host
             app). The alias maps that component name to our trampoline, which opens
             the host app's own launcher — so tapping a notification opens the app. -->
        <activity
            android:name="com.meshsdk.NotificationLauncherActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity-alias
            android:name="com.bitchat.android.MainActivity"
            android:exported="false"
            android:targetActivity="com.meshsdk.NotificationLauncherActivity" />
    </application>

</manifest>
